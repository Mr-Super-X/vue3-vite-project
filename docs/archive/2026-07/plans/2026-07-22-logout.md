# Logout 现代化重构 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `userStore.logout()` 改造为悲观退出流程（先等后端 + 全栈清理 + AbortController 取消在途请求），并在 Dashboard 顶部右上绑定退出按钮。

**Architecture:**

- `userStore.logout()` 改 async：先 `await authApi.logout()`（失败由 http.ts 拦截器 toast + 抛 ApiError 中断），成功才清本地状态。
- 新增 `src/api/global-abort.ts` 单例 + `chainSignals()` helper；http.ts 请求拦截器合并 per-request + global signal。
- 新增 `src/composables/useLogout.ts` 封装 ElMessageBox.confirm + loading + store 调用；Header.vue 与 Dashboard Index.vue 复用。
- mock 加 `/api/auth/logout`；i18n 加 logout 翻译。

**Tech Stack:** Pinia · Vue Router · Element Plus · axios · AbortController · vitest · Session/clearCookies（`utils/storage.ts`）

---

## Phase 0：备份与前置

### Task 0.1：建立备份目录与清单

**Files:**

- Create: `.claude/backups/logout-2026-07-22/manifest.txt`

- [ ] **Step 1：创建目录与清单**

```bash
mkdir -p ".claude/backups/logout-2026-07-22"
```

清单内容写入 `.claude/backups/logout-2026-07-22/manifest.txt`：

```text
[MODIFY]
D:\work\应急水利\应急\vue3-vite-project\src\store\modules\user.ts
D:\work\应急水利\应急\vue3-vite-project\src\api\http.ts
D:\work\应急水利\应急\vue3-vite-project\src\components\layout\Header.vue
D:\work\应急水利\应急\vue3-vite-project\src\modules\dashboard\views\Index.vue
D:\work\应急水利\应急\vue3-vite-project\mock\auth.ts
D:\work\应急水利\应急\vue3-vite-project\src\locales\zh-CN.ts
D:\work\应急水利\应急\vue3-vite-project\src\locales\en-US.ts
D:\work\应急水利\应急\vue3-vite-project\CHANGELOG.md

[NEW]
D:\work\应急水利\应急\vue3-vite-project\src\api\global-abort.ts
D:\work\应急水利\应急\vue3-vite-project\src\api\global-abort.spec.ts
D:\work\应急水利\应急\vue3-vite-project\src\composables\useLogout.ts
D:\work\应急水利\应急\vue3-vite-project\src\composables\useLogout.spec.ts
D:\work\应急水利\应急\vue3-vite-project\src\store\modules\user.spec.ts
D:\work\应急水利\应急\vue3-vite-project\docs\superpowers\plans\2026-07-22-logout.md
D:\work\应急水利\应急\vue3-vite-project\docs\superpowers\specs\2026-07-22-logout-design.md
```

- [ ] **Step 2：备份当前 user.ts**

```bash
cp "D:\work\应急水利\应急\vue3-vite-project\src\store\modules\user.ts" ".claude/backups\logout-2026-07-22\user.ts.bak"
```

---

## Phase 1：基础设施层

### Task 1.1：新增 `src/api/global-abort.ts`

**Files:**

- Create: `D:\work\应急水利\应急\vue3-vite-project\src\api\global-abort.ts`
- Test: `D:\work\应急水利\应急\vue3-vite-project\src\api\global-abort.spec.ts`

- [ ] **Step 1：写失败的测试**

文件 `src/api/global-abort.spec.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { globalAbort, chainSignals } from './global-abort'

describe('chainSignals', () => {
  it('无信号时返回永不中止的占位 signal', () => {
    const s = chainSignals()
    expect(s.aborted).toBe(false)
  })

  it('单个 undefined 被过滤', () => {
    const s = chainSignals(undefined)
    expect(s.aborted).toBe(false)
  })

  it('单个 signal 透传', () => {
    const c = new AbortController()
    const s = chainSignals(c.signal)
    expect(s).toBe(c.signal)
  })

  it('多 signal 合并：任一触发即中止', () => {
    const c1 = new AbortController()
    const c2 = new AbortController()
    const s = chainSignals(c1.signal, c2.signal)
    expect(s.aborted).toBe(false)
    c1.abort()
    expect(s.aborted).toBe(true)
  })

  it('多 signal 合并：另一信号触发也中止', () => {
    const c1 = new AbortController()
    const c2 = new AbortController()
    const s = chainSignals(c1.signal, c2.signal)
    c2.abort('second')
    expect(s.aborted).toBe(true)
    expect(s.reason).toBe('second')
  })
})

describe('globalAbort', () => {
  it('初始未中止', () => {
    expect(globalAbort.signal.aborted).toBe(false)
  })

  it('abort() 后 signal.aborted = true', () => {
    // 注意：单例状态依赖测试顺序，此处只验证一次副作用
    const reason = 'logout-test-' + Date.now()
    globalAbort.abort(reason)
    expect(globalAbort.signal.aborted).toBe(true)
    expect(globalAbort.signal.reason).toBe(reason)
    // 重置以便不影响其他用例
    globalAbort.reset()
  })

  it('reset() 后 signal 重新可观察', () => {
    globalAbort.abort('temp')
    globalAbort.reset()
    expect(globalAbort.signal.aborted).toBe(false)
    expect(globalAbort.signal.reason).toBe(undefined)
  })

  it('重复 abort() 不会抛错', () => {
    globalAbort.abort('first')
    expect(() => globalAbort.abort('second')).not.toThrow()
    globalAbort.reset()
  })
})
```

- [ ] **Step 2：运行测试确认失败**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/api/global-abort 2>&1 | tail -20
```

Expected: 失败，提示模块未找到。

- [ ] **Step 3：写实现**

文件 `src/api/global-abort.ts`：

```ts
/**
 * 全局 AbortController 单例 + signal 合并工具。
 *
 * 用途：logout 等需要"一次性取消所有在途请求"的场景。
 * 单例 signal 注入 http.ts 请求拦截器，logout 时统一 abort()。
 */

class GlobalAbortController {
  private controller = new AbortController()

  get signal(): AbortSignal {
    return this.controller.signal
  }

  /** 若当前 signal 已 aborted，创建新 controller；否则 no-op */
  reset(): void {
    if (this.controller.signal.aborted) {
      this.controller = new AbortController()
    }
  }

  /** 幂等：已 aborted 时再次调用不会抛错 */
  abort(reason?: string): void {
    if (!this.controller.signal.aborted) {
      this.controller.abort(reason)
    }
  }
}

export const globalAbort = new GlobalAbortController()

/**
 * 合并多个 AbortSignal，任一触发即中止。
 * - 全 undefined：返回永不 abort 的占位 signal（避免 AbortSignal.any([]) 抛错）
 * - 单个：透传
 * - 多个：AbortSignal.any() 合并
 */
export function chainSignals(...signals: (AbortSignal | undefined)[]): AbortSignal {
  const filtered = signals.filter((s): s is AbortSignal => s !== undefined)
  if (filtered.length === 0) {
    return new AbortController().signal
  }
  if (filtered.length === 1) return filtered[0]!
  return AbortSignal.any(filtered)
}
```

- [ ] **Step 4：运行测试确认通过**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/api/global-abort 2>&1 | tail -10
```

Expected: 8 个用例全部通过。

- [ ] **Step 5：commit**

```bash
git add src/api/global-abort.ts src/api/global-abort.spec.ts
git commit -m "feat(api): add globalAbort singleton + chainSignals helper"
```

---

### Task 1.2：在 `http.ts` 请求拦截器中注入 globalAbort signal

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\api\http.ts:22-33`

- [ ] **Step 1：修改请求拦截器**

打开 `src/api/http.ts`，将现有的：

```ts
instance.interceptors.request.use((config) => {
  // Session.get('token') 在生产环境强制 secure + sameSite=lax，
  // 与 utils/storage.ts 的 token 存储约定一致。
  const token = Session.get<string>('token')
  if (typeof token === 'string' && token.length > 0) {
    config.headers.set('Authorization', `Bearer ${token}`)
  }
  return config
})
```

替换为：

```ts
import { globalAbort, chainSignals } from './global-abort'

// ...

instance.interceptors.request.use((config) => {
  // Session.get('token') 在生产环境强制 secure + sameSite=lax，
  // 与 utils/storage.ts 的 token 存储约定一致。
  const token = Session.get<string>('token')
  if (typeof token === 'string' && token.length > 0) {
    config.headers.set('Authorization', `Bearer ${token}`)
  }
  // 合并 per-request signal 与 globalAbort signal；
  // logout 时 globalAbort.abort() 会取消所有在途请求。
  config.signal = chainSignals(config.signal, globalAbort.signal)
  return config
})
```

- [ ] **Step 2：补充 http.spec.ts 用例**

打开 `src/api/http.spec.ts`，在 `describe('请求拦截器', ...)` 块中追加：

```ts
it('请求拦截器合并 globalAbort signal', () => {
  const handler = getRequestHandler()
  // 取当前的 globalAbort.signal 引用，handler.onFulfilled 后应当被合并进 cfg.signal
  const beforeSignal = globalAbort.signal
  const cfg = { headers: new Map<string, string>(), signal: undefined as AbortSignal | undefined }
  handler.onFulfilled(cfg)
  // 合并后 cfg.signal 与 globalAbort.signal 是同一引用或被 chainSignals 包装；
  // 简化断言：abort globalAbort 后 cfg.signal 也应 aborted
  beforeSignal.abort('http-spec-test')
  expect(cfg.signal?.aborted).toBe(true)
  // 复位不影响其他用例
  globalAbort.reset()
})
```

并在文件顶部 import：

```ts
import { globalAbort } from './global-abort'
```

- [ ] **Step 3：运行测试确认通过**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/api/http 2>&1 | tail -10
```

Expected: 全部通过（新增 1 个用例）。

- [ ] **Step 4：commit**

```bash
git add src/api/http.ts src/api/http.spec.ts
git commit -m "feat(http): merge globalAbort signal into request interceptor"
```

---

## Phase 2：业务核心层

### Task 2.1：mock 加 `/api/auth/logout`

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\mock\auth.ts`

- [ ] **Step 1：追加 mock 条目**

打开 `mock\auth.ts`，在现有数组末尾（`export default [...] as MockMethod[]`）追加新条目：

```ts
{
  url: '/api/auth/logout',
  method: 'post',
  timeout: 100,
  response: () => ({
    code: 0,
    message: 'ok',
    data: null,
  }),
},
```

- [ ] **Step 2：手动验证**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm dev  # 起 dev server
```

另开终端：

```bash
curl -X POST http://localhost:5173/api/auth/logout
```

Expected: `{"code":0,"message":"ok","data":null}`（dev 服务通过 vite-plugin-mock 拦截）。

- [ ] **Step 3：commit**

```bash
git add mock/auth.ts
git commit -m "feat(mock): add /api/auth/logout mock endpoint"
```

---

### Task 2.2：改造 `userStore.logout()`（悲观 + 全栈清理）

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\store\modules\user.ts`

- [ ] **Step 1：写失败的 store 测试**

新建 `src/store/modules/user.spec.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { createPinia, setActivePinia } from 'pinia'

// vue-router 必须 mock：userStore.logout() 内部调用 useRouter().push
const mockPush = vi.fn().mockResolvedValue(undefined)
vi.mock('vue-router', () => ({
  useRouter: () => ({ push: mockPush }),
}))

// useRouterStore mock（其 $reset 方法也需要可观察）
const mockRouterStoreReset = vi.fn()
const mockUseRouterStore = vi.fn(() => ({ $reset: mockRouterStoreReset }))
vi.mock('./router', () => ({
  useRouterStore: () => mockUseRouterStore(),
}))

// 其余依赖 mock
vi.mock('@/api/modules/auth', () => ({
  authApi: {
    login: vi.fn(),
    fetchProfile: vi.fn(),
    logout: vi.fn(),
  },
}))
vi.mock('@/utils/storage', () => ({
  Session: { remove: vi.fn(), set: vi.fn(), get: vi.fn() },
  clearCookies: vi.fn(),
  Local: { set: vi.fn(), get: vi.fn(), remove: vi.fn(), clear: vi.fn() },
}))
vi.mock('@/api/global-abort', () => ({
  globalAbort: {
    signal: new AbortController().signal,
    abort: vi.fn(),
    reset: vi.fn(),
  },
}))
vi.mock('@/router/guards/auth', () => ({
  resetRouterState: vi.fn(),
}))

// 必须放在 mock 之后
import { useUserStore } from './user'
import { authApi } from '@/api/modules/auth'
import { Session, clearCookies } from '@/utils/storage'
import { globalAbort } from '@/api/global-abort'
import { resetRouterState } from '@/router/guards/auth'

const mockAuthLogout = authApi.logout as ReturnType<typeof vi.fn>
const mockSessionRemove = Session.remove as ReturnType<typeof vi.fn>
const mockClearCookies = clearCookies as ReturnType<typeof vi.fn>
const mockGlobalAbort = globalAbort.abort as ReturnType<typeof vi.fn>
const mockResetRouter = resetRouterState as ReturnType<typeof vi.fn>

beforeEach(() => {
  setActivePinia(createPinia())
  mockAuthLogout.mockReset()
  mockSessionRemove.mockReset()
  mockClearCookies.mockReset()
  mockGlobalAbort.mockReset()
  mockResetRouter.mockReset()
  mockRouterStoreReset.mockReset()
  mockPush.mockReset()
  mockUseRouterStore.mockClear()
})

describe('userStore.logout()', () => {
  it('成功路径：先调后端 logout，再清本地 + 跳转', async () => {
    mockAuthLogout.mockResolvedValueOnce(undefined)
    const store = useUserStore()
    store.token = 'mock'
    store.profile = { id: 1, name: 'A', permissions: ['p1'] }
    store.permissions = ['p1']

    await store.logout()

    expect(mockAuthLogout).toHaveBeenCalledTimes(1)
    expect(mockSessionRemove).toHaveBeenCalledWith('token')
    expect(mockClearCookies).toHaveBeenCalledTimes(1)
    expect(mockGlobalAbort).toHaveBeenCalledWith('logout')
    expect(mockResetRouter).toHaveBeenCalledTimes(1)
    expect(mockRouterStoreReset).toHaveBeenCalledTimes(1)
    expect(mockPush).toHaveBeenCalledWith('/login')
    expect(store.token).toBe('')
    expect(store.profile).toBe(null)
    expect(store.permissions).toEqual([])
  })

  it('失败路径：后端 logout 抛错时不执行任何清理', async () => {
    const apiError = new Error('server error')
    mockAuthLogout.mockRejectedValueOnce(apiError)
    const store = useUserStore()
    store.token = 'mock'

    await expect(store.logout()).rejects.toThrow('server error')

    expect(mockSessionRemove).not.toHaveBeenCalled()
    expect(mockClearCookies).not.toHaveBeenCalled()
    expect(mockGlobalAbort).not.toHaveBeenCalled()
    expect(mockResetRouter).not.toHaveBeenCalled()
    expect(mockRouterStoreReset).not.toHaveBeenCalled()
    expect(mockPush).not.toHaveBeenCalled()
    expect(store.token).toBe('mock') // 保留
  })
})
```

- [ ] **Step 2：运行测试确认失败**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/store/modules/user 2>&1 | tail -30
```

Expected: 失败（当前 logout 不会调 authApi.logout）。

- [ ] **Step 3：改造 user.ts**

打开 `src/store/modules/user.ts`，替换为：

```ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'
import { authApi, type LoginPayload, type UserProfile } from '@/api/modules/auth'
import { Session, clearCookies } from '@/utils/storage'
import { globalAbort } from '@/api/global-abort'
import { resetRouterState } from '@/router/guards/auth'
import { useRouterStore } from './router'

export const useUserStore = defineStore('user', () => {
  const token = ref<string>(Session.get<string>('token') ?? '')
  const profile = ref<UserProfile | null>(null)
  const permissions = ref<string[]>([])

  const isLoggedIn = computed(() => !!token.value)

  async function login(credentials: LoginPayload) {
    const { token: t } = await authApi.login(credentials)
    token.value = t
    Session.set('token', t)
    await fetchProfile()
  }

  async function fetchProfile() {
    const p = await authApi.fetchProfile()
    profile.value = p
    permissions.value = p.permissions
  }

  /**
   * 悲观退出：先 await 后端 /auth/logout（失败由 http.ts 拦截器 toast + 抛 ApiError 中断），
   * 成功才清本地状态 + 取消在途请求 + 重置路由模块状态 + 跳登录页。
   */
  async function logout(): Promise<void> {
    await authApi.logout()
    Session.remove('token')
    clearCookies()
    token.value = ''
    profile.value = null
    permissions.value = []
    globalAbort.abort('logout')
    resetRouterState()
    const routerStore = useRouterStore()
    if (typeof routerStore.$reset === 'function') {
      routerStore.$reset()
    }
    const router = useRouter()
    await router.push('/login')
  }

  return { token, profile, permissions, isLoggedIn, login, fetchProfile, logout }
})
```

- [ ] **Step 4：运行测试确认通过**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/store/modules/user 2>&1 | tail -15
```

Expected: 2 个用例通过。

- [ ] **Step 5：commit**

```bash
git add src/store/modules/user.ts src/store/modules/user.spec.ts
git commit -m "feat(store): pessimistic logout + full state cleanup"
```

---

### Task 2.3：新增 `src/composables/useLogout.ts`

**Files:**

- Create: `D:\work\应急水利\应急\vue3-vite-project\src\composables\useLogout.ts`
- Create: `D:\work\应急水利\应急\vue3-vite-project\src\composables\useLogout.spec.ts`

- [ ] **Step 1：写失败的测试**

文件 `src/composables/useLogout.spec.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { ref } from 'vue'

vi.mock('element-plus', () => ({
  ElMessageBox: { confirm: vi.fn() },
}))

const mockStoreLogout = vi.fn()

vi.mock('@/store/modules/user', () => ({
  useUserStore: () => ({ logout: mockStoreLogout }),
}))

import { ElMessageBox } from 'element-plus'
import { useLogout } from './useLogout'

beforeEach(() => {
  mockStoreLogout.mockReset()
  ;(ElMessageBox.confirm as ReturnType<typeof vi.fn>).mockReset()
})

describe('useLogout', () => {
  it('confirm 取消时 store.logout 不调用', async () => {
    ;(ElMessageBox.confirm as ReturnType<typeof vi.fn>).mockRejectedValueOnce(new Error('cancel'))
    const { loggingOut, confirmLogout } = useLogout()
    await confirmLogout()
    expect(mockStoreLogout).not.toHaveBeenCalled()
    expect(loggingOut.value).toBe(false)
  })

  it('confirm 确认 + store 成功时 loading 切换正确', async () => {
    ;(ElMessageBox.confirm as ReturnType<typeof vi.fn>).mockResolvedValueOnce('ok')
    mockStoreLogout.mockResolvedValueOnce(undefined)
    const { loggingOut, confirmLogout } = useLogout()
    const p = confirmLogout()
    // loggingOut 应在调用期间为 true
    // 注意：这里无法同步读取，只能验证最终状态
    await p
    expect(loggingOut.value).toBe(false)
    expect(mockStoreLogout).toHaveBeenCalledTimes(1)
  })

  it('store 抛错时 loggingOut 在 finally 中复位', async () => {
    ;(ElMessageBox.confirm as ReturnType<typeof vi.fn>).mockResolvedValueOnce('ok')
    mockStoreLogout.mockRejectedValueOnce(new Error('boom'))
    const { loggingOut, confirmLogout } = useLogout()
    await expect(confirmLogout()).rejects.toThrow('boom')
    expect(loggingOut.value).toBe(false)
  })

  it('初始 loggingOut 为 false', () => {
    const { loggingOut } = useLogout()
    expect(loggingOut.value).toBe(false)
  })
})
```

- [ ] **Step 2：运行测试确认失败**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/composables/useLogout 2>&1 | tail -15
```

Expected: 失败（模块不存在）。

- [ ] **Step 3：写实现**

文件 `src/composables/useLogout.ts`：

```ts
import { ref } from 'vue'
import { ElMessageBox } from 'element-plus'
import { useUserStore } from '@/store/modules/user'

/**
 * 退出登录 composable。
 *
 * 封装 ElMessageBox.confirm 二次确认 + loading 态 + store.logout 调用。
 * Header.vue 与 Dashboard Index.vue 复用，避免重复实现确认逻辑。
 */
export function useLogout() {
  const userStore = useUserStore()
  const loggingOut = ref(false)

  async function confirmLogout(): Promise<void> {
    try {
      await ElMessageBox.confirm('确定退出登录吗？', '提示', {
        confirmButtonText: '退出',
        cancelButtonText: '取消',
        type: 'warning',
      })
    } catch {
      // 用户在 confirm 弹窗点取消
      return
    }

    loggingOut.value = true
    try {
      await userStore.logout()
    } finally {
      loggingOut.value = false
    }
  }

  return { loggingOut, confirmLogout }
}
```

- [ ] **Step 4：运行测试确认通过**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test src/composables/useLogout 2>&1 | tail -10
```

Expected: 4 个用例通过。

- [ ] **Step 5：commit**

```bash
git add src/composables/useLogout.ts src/composables/useLogout.spec.ts
git commit -m "feat(composable): add useLogout with confirm + loading"
```

---

## Phase 3：UI 接入

### Task 3.1：改造 `Header.vue`

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\components\layout\Header.vue`

- [ ] **Step 1：替换 script + template**

将 `Header.vue` 的 `<script setup>` 与 `<template>` 部分替换：

```vue
<script setup lang="ts">
import { createNamespace } from '@utils/bem'
import { useUserStore } from '@/store/modules/user'
import { useLogout } from '@/composables/useLogout'

const userStore = useUserStore()
// 运行时 BEM 命名空间：生成 vv-header-bar / vv-header-bar__user / vv-header-bar__action / is-logged-out
// 与 <style> 块的 SCSS mixin 命名完全对齐，便于阅读与维护。
const bem = createNamespace('header-bar')
const { loggingOut, confirmLogout } = useLogout()
</script>

<template>
  <div :class="[bem.b(), 'flex-between', bem.is('logged-out', !userStore.isLoggedIn)]">
    <span :class="bem.e('user')">
      {{ userStore.profile?.name ?? '游客' }}
    </span>
    <el-button :class="bem.e('action')" text :loading="loggingOut" @click="confirmLogout">
      退出
    </el-button>
  </div>
</template>
```

- [ ] **Step 2：commit**

```bash
git add src/components/layout/Header.vue
git commit -m "feat(Header): bind useLogout with loading state"
```

---

### Task 3.2：Dashboard Index.vue 顶部右上加退出按钮

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\modules\dashboard\views\Index.vue`

- [ ] **Step 1：替换内容**

```vue
<script setup lang="ts">
import { useLogout } from '@/composables/useLogout'

const { loggingOut, confirmLogout } = useLogout()
</script>

<template>
  <div class="dashboard">
    <header class="dashboard__header">
      <h1>{{ $t('menu.dashboard') }}</h1>
      <el-button
        type="warning"
        plain
        :loading="loggingOut"
        @click="confirmLogout"
      >
        退出登录
      </el-button>
    </header>
    <el-row :gutter="16">
      <el-col :span="8"><el-card>用户总数：128</el-card></el-col>
      <el-col :span="8"><el-card>在线用户：12</el-card></el-col>
      <el-col :span="8"><el-card>今日访问：256</el-card></el-card>
    </el-row>
  </div>
</template>

<style lang="scss" scoped>
.dashboard {
  &__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
  }
}
</style>
```

- [ ] **Step 2：commit**

```bash
git add src/modules/dashboard/views/Index.vue
git commit -m "feat(Dashboard): add top-right logout button"
```

---

### Task 3.3：i18n 翻译键

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\locales\zh-CN.ts`
- Modify: `D:\work\应急水利\应急\vue3-vite-project\src\locales\en-US.ts`

- [ ] **Step 1：zh-CN.ts 加 logout 键**

```ts
export default {
  app: { title: '企业中后台管理' },
  common: { confirm: '确认', cancel: '取消', retry: '重试', loading: '加载中...' },
  auth: {
    login: '登录',
    logout: '退出',
    logoutConfirm: '确定退出登录吗？',
    logoutConfirmButton: '退出',
    logoutCancelButton: '取消',
    logoutTitle: '提示',
    username: '用户名',
    password: '密码',
  },
  menu: { dashboard: '仪表盘', user: '用户管理' },
  error: { '403': '无权访问', '404': '页面不存在', '500': '服务器错误' },
}
```

- [ ] **Step 2：en-US.ts 加 logout 键**

```ts
export default {
  app: { title: 'Emergency Water Portal' },
  common: { confirm: 'Confirm', cancel: 'Cancel', retry: 'Retry', loading: 'Loading...' },
  auth: {
    login: 'Login',
    logout: 'Logout',
    logoutConfirm: 'Are you sure you want to log out?',
    logoutConfirmButton: 'Logout',
    logoutCancelButton: 'Cancel',
    logoutTitle: 'Notice',
    username: 'Username',
    password: 'Password',
  },
  menu: { dashboard: 'Dashboard', user: 'User Management' },
  error: { '403': 'Forbidden', '404': 'Not Found', '500': 'Server Error' },
}
```

- [ ] **Step 3：commit**

```bash
git add src/locales/zh-CN.ts src/locales/en-US.ts
git commit -m "feat(i18n): add logout confirm keys (zh-CN/en-US)"
```

---

## Phase 4：文档收尾

### Task 4.1：CHANGELOG 追加

**Files:**

- Modify: `D:\work\应急水利\应急\vue3-vite-project\CHANGELOG.md`

- [ ] **Step 1：在 Unreleased > Changed 段末尾追加（若已有 Changed 段则合并）**

```markdown
- 改造 `src/store/modules/user.ts`：logout() 改 async 悲观语义（先 await 后端 /auth/logout，失败由 http.ts 拦截器 toast + 抛 ApiError 中断；成功才清本地状态）。新增调用顺序：Session.remove('token') → clearCookies() → 清 ref → globalAbort.abort('logout') 取消在途请求 → resetRouterState() → useRouterStore().$reset() → router.push('/login')
- 改造 `src/api/http.ts` 请求拦截器：合并 per-request signal 与 `globalAbort.signal`，logout 时统一取消所有在途请求
- 改造 `src/components/layout/Header.vue`：复用 `useLogout()` composable，绑定 `confirmLogout` + `loggingOut` loading
- 改造 `src/modules/dashboard/views/Index.vue`：顶部右上加 `退出登录` 按钮（type=warning plain），同样绑定 useLogout
- 改造 `mock/auth.ts`：新增 `/api/auth/logout` mock 条目（之前缺失，导致真实 dev 调用 404）
- 改造 `src/locales/{zh-CN,en-US}.ts`：在 `auth` 段加 `logoutConfirm` / `logoutConfirmButton` / `logoutCancelButton` / `logoutTitle` 翻译键

### Added

- 新增 `src/api/global-abort.ts`：`GlobalAbortController` 单例 + `chainSignals(...signals)` 合并工具（无信号返回占位 / 单个透传 / 多个 `AbortSignal.any()`）
- 新增 `src/composables/useLogout.ts`：封装 ElMessageBox.confirm 二次确认 + `loggingOut` ref + store.logout 调用，Header 与 Dashboard 复用
- 新增 `src/api/global-abort.spec.ts`：8 个用例覆盖 chainSignals（4）+ globalAbort 单例（4）
- 新增 `src/composables/useLogout.spec.ts`：4 个用例覆盖确认取消 / 成功路径 / store 抛错 / 初始 loading
- 新增 `src/store/modules/user.spec.ts`：2 个用例覆盖 logout 成功路径（全栈清理 + 跳转）与失败路径（不执行任何清理）
```

- [ ] **Step 2：commit**

```bash
git add CHANGELOG.md
git commit -m "docs(changelog): logout modernization entry"
```

---

## Phase 5：最终验证

### Task 5.1：全量验证

- [ ] **Step 1：运行全量 vitest**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm test 2>&1 | tail -15
```

Expected: 所有测试通过（≥ 56 个用例：原有 52 + 新增 4 store + 4 composable + 1 http signal 合并）。

- [ ] **Step 2：type-check**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm type-check 2>&1 | tail -10
```

Expected: 无错误。

- [ ] **Step 3：lint**

```bash
cd "D:\work\应急水利\应急\vue3-vite-project" && pnpm lint 2>&1 | tail -10
```

Expected: 无错误。

- [ ] **Step 4：手动验证（用户执行）**

| 场景                  | 操作                                               | 预期                                            |
| --------------------- | -------------------------------------------------- | ----------------------------------------------- |
| 登录后 Header 退出    | 登录 → 点 Header "退出" → confirm 确认             | 弹 confirm → 确认 → loading → toast → 跳 /login |
| 登录后 Dashboard 退出 | 登录 → 进 Dashboard → 点 "退出登录" → confirm 确认 | 同上                                            |
| confirm 取消          | 弹窗点取消                                         | 不退出，无变化                                  |
| 网络中断退出          | 登录 → DevTools Network throttle offline → 点退出  | 15s 内弹 "网络异常"，留原页                     |
| 在途请求被取消        | 登录 → 触发慢请求（可手动 mock 500ms）→ 立即点退出 | Network 面板看到慢请求 canceled                 |
| 持久化保留            | 登录 → 切 dark mode → 退出 → 重开浏览器            | 仍是 dark mode                                  |
| 重复点击              | 加载中快速双击退出                                 | 第二次点击无效（loading 态 disabled）           |

---

## 单测覆盖矩阵

| 文件                                | 用例数 | 覆盖点                                             |
| ----------------------------------- | ------ | -------------------------------------------------- |
| `src/api/global-abort.spec.ts`      | 8      | chainSignals（4 边界）+ globalAbort 单例（4 行为） |
| `src/api/http.spec.ts`              | 12     | 原有 11 + 新增 1（globalAbort signal 合并）        |
| `src/composables/useLogout.spec.ts` | 4      | 取消 / 成功 / 抛错 / 初始 loading                  |
| `src/store/modules/user.spec.ts`    | 2      | 成功路径（全栈清理 + 跳转）/ 失败路径（不清理）    |
| **合计新增**                        | **15** |                                                    |

## 风险与回退

| 风险                             | 缓解                                                        |
| -------------------------------- | ----------------------------------------------------------- |
| `AbortSignal.any()` 浏览器兼容   | 现代浏览器全支持（Chrome 100+ / Firefox 100+ / Safari 15+） |
| `useRouterStore().$reset` 不存在 | 已用 `typeof === 'function'` 守卫；不存在则跳过             |
| `globalAbort` 单例状态泄漏       | 仅在 logout 时 abort，下次登录后请求自然通过新 controller   |
| mock 缺失导致 dev 调用 404       | Phase 2.1 已新增 mock 条目                                  |

回退方案：

```bash
git checkout HEAD~ -- src/store/modules/user.ts src/components/layout/Header.vue src/api/http.ts src/modules/dashboard/views/Index.vue mock/auth.ts src/locales/zh-CN.ts src/locales/en-US.ts
rm src/api/global-abort.ts src/api/global-abort.spec.ts src/composables/useLogout.ts src/composables/useLogout.spec.ts src/store/modules/user.spec.ts
```

---

## Commit 序列汇总

按任务顺序共 7 个 commit：

1. `feat(api): add globalAbort singleton + chainSignals helper`
2. `feat(http): merge globalAbort signal into request interceptor`
3. `feat(mock): add /api/auth/logout mock endpoint`
4. `feat(store): pessimistic logout + full state cleanup`
5. `feat(composable): add useLogout with confirm + loading`
6. `feat(Header): bind useLogout with loading state`
7. `feat(Dashboard): add top-right logout button`
8. `feat(i18n): add logout confirm keys (zh-CN/en-US)`
9. `docs(changelog): logout modernization entry`

---

文档版本：v1.0 | 2026-07-22
