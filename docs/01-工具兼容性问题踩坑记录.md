# 工具兼容性问题踩坑记录（索引）

> 本文档是项目踩坑记录的**索引页**。完整归档迁移到 `docs/superpowers/specs/` 下的独立 spec 文件。
>
> **维护原则**：
>
> - 每条问题独立编号 `YYYY-MM-DD-NNN`
> - 完整证据（堆栈 / 日志 / 环境 / 复现 / 根因 / 修复 / 教训）放在 `docs/superpowers/specs/YYYY-MM-DD-<slug>.md`
> - 索引页仅保留编号 + 链接 + 严重程度 + 状态，避免索引膨胀
> - 同一问题出现新进展（如 npm 12 修复）→ 追加到对应 spec 末尾，不新增编号
>
> **最后更新**：2026-09-11（迁移原 `2026-07-20-001` 到 `docs/superpowers/specs/2026-07-20-npm-pnpm-compatibility.md`）

---

## 索引

| 编号           | 日期       | 工具组合            | 严重程度 | 状态                 | 详细归档                                                      |
| -------------- | ---------- | ------------------- | -------- | -------------------- | ------------------------------------------------------------- |
| 2026-07-20-001 | 2026-07-20 | npm 10/11 + pnpm 11 | 高       | 已记录，缓解方案已知 | [→](./superpowers/specs/2026-07-20-npm-pnpm-compatibility.md) |

---

## 新增问题流程

1. 在 `docs/superpowers/specs/` 新建 `YYYY-MM-DD-<slug>.md`，文件头按归档模板（参考 `2026-07-20-npm-pnpm-compatibility.md`）：类型 / 日期 / 原始位置 / 状态 / 摘要
2. 在本文档索引表加一行，**编号与 spec 文件名保持一致**
3. CHANGELOG 加一条 `docs:` 条目

## 历史背景

本索引页迁移自 2026-09-11 之前的 `docs/01-工具兼容性问题踩坑记录.md`。原文档只有 1 条记录（npm install 在 pnpm 项目中崩溃），详情已迁移到 `docs/superpowers/specs/2026-07-20-npm-pnpm-compatibility.md`，本索引页仅保留链接。

后续如需新增问题，请严格按「新增问题流程」执行。
